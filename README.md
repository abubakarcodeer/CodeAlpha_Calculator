# Calculator App

A simple **Calculator web application** built using **HTML, CSS, and JavaScript**.  
This calculator uses JavaScript’s `eval()` function to evaluate expressions and includes **clear** and **delete** functionality for better user control.

## 🌟 Features

- ➕➖✖️➗ Basic arithmetic operations
- 🧮 Expression evaluation using JavaScript `eval()`
- ⌫ **Delete** button to remove the last input
- 🧹 **Clear** button to reset the calculator
- 🎨 Clean and simple calculator UI
- ⚡ Instant result calculation

## 🧱 Technologies Used

- **HTML** – Structure of the calculator  
- **CSS** – Styling and layout  
- **JavaScript** – Calculation logic and button handling  


## 🧠 How It Works

1. User clicks number and operator buttons
2. Expression is displayed on the screen
3. On pressing `=`, the expression is calculated using `eval()`
4. **Delete** button removes the last character
5. **Clear** button resets the entire input

## 🚀 Getting Started

### 1. Clone the repository
```bash
git clone https://github.com/abubakarcodeer/CodeAlpha_Calculator.git
